# metallb-ip-pool.yaml
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: on-prem-pool
  namespace: metallb-system
spec:
  addresses:
  - 192.168.1.60-192.168.1.90 # Replace with your dedicated IP range
  autoAssign: true
